# Atlas Memory – 06.03.2026

## GitHub Actions Runner Setup (abgeschlossen)

### Ergebnis
- 4 Self-Hosted Runner laufen dauerhaft auf llm.local:
  - `github-runner-1` + `github-runner-2` (Docker, Linux, myoung34/github-runner)
  - `windows-runner-1` + `windows-runner-2` (Windows nativ)
- Scope: **Org-Level** (Minicon-eG) → alle Repos nutzbar
- Auth: GitHub App (ID: 2973586, Install: 113095606), PEM: `C:\working\georg\github-runners\runner-app.pem`

### Wichtige Erkenntnisse
- Org-Level Token Endpoint: `/orgs/Minicon-eG/actions/runners/registration-token` ✅
- Repo-Level Endpoint funktioniert NICHT für Org-Runner → war anfangs falsch eingerichtet
- Windows-Runner verlieren Registrierung nach längerer Offline-Zeit → `start-windows-runners.ps1` macht auto remove+reconfigure
- `runner-app.pem` ist im privaten Repo, daher eingecheckt (OK)

### Autostart
- Task Scheduler `\Minicon\GitHub-Windows-Runners` (30s Delay)
- Task Scheduler `\Minicon\GitHub-Docker-Runners` (60s Delay)
- Logs: `C:\working\georg\logs\`

### Versionierung
- Scripts: `Minicon-eG/infrastructure` → `github-runner/scripts/`
- Zuvor fälschlicherweise in `nikolausm/clawdbot-agents` (bereinigt)
- `runner-app.pem` aus clawdbot-agents entfernt + .gitignore gesetzt

### Dokumentation
- Confluence: https://minicon.atlassian.net/wiki/spaces/MINICON/pages/844562433
- Verschoben unter: Technisches Betriebshandbuch - Minicon Hub
- Gegenseitig verlinkt mit Deploy-Workflow-Seite (843350017)

---

## CI/CD Vereinheitlichung (Kundenportal + Website-Service)

### Ausgangslage
- `minicon-kundenportal` und `minicon-website-service` nutzten `ubuntu-latest` (GitHub-hosted)
- 40+ Zeilen custom Workflow-Code je Repo

### Ergebnis
- Beide nutzen jetzt `deploy-docker-service.yml` Reusable Workflow aus `Minicon-eG/.github`
- Runner: `self-hosted, linux` (lokale Runner)
- Reusable Workflow erweitert: `ports`, `server`, `no_cache` Parameter hinzugefügt
- Preview-Branch Support: `preview-mein.minicon.eu` + `preview-api.minicon.eu`

### Traefik routes.yml
- Preview-Routen mit Priorität 200 für `preview-mein` und `preview-api` ergänzt
- CORS: `preview-mein.minicon.eu` als erlaubte Origin hinzugefügt
- **Jetzt versioniert:** `Minicon-eG/infrastructure` → `minicon-web/traefik/routes.yml`
- Deploy-Script: `.\minicon-web\sync-traefik.ps1` (scp + SIGHUP)
- Confluence Deploy-Workflow-Seite verweist auf Git-Repo

---

## Infra-Repo Minicon-eG/infrastructure (neu erstellt heute)

Struktur:
```
infrastructure/
├── github-runner/
│   ├── docker-compose.yml      (Org-Level Runner)
│   ├── runner-app.pem          (GitHub App Private Key)
│   └── scripts/
│       ├── renew-runner-token.ps1
│       ├── start-docker-runners.ps1
│       └── start-windows-runners.ps1
└── minicon-web/
    ├── sync-traefik.ps1
    └── traefik/
        ├── routes.yml
        └── README.md
```

---

## OpenClaw Logs – Beobachtungen

- Memory-Search deaktiviert (Gemini API Key abgelaufen → Embeddings fehlen)
- MiniMax-Portal 401 Fehler bei einem Cron-Job heute früh (API Key war kurzzeitig ungültig)
- `kundenportal.minicon.eu` vom Hub-Status-Cron als "DOWN" gemeldet → nicht deployed, existiert nicht als Subdomain (war Fehlalarm durch lokales Verzeichnis)

---

## Offene Punkte
- Preview-Subdomains (preview-mein, preview-api) sind Traefik-seitig konfiguriert, aber noch nie deployed
- Gemini API Key erneuern (Embeddings/Memory-Search ausgefallen)
- Task Scheduler zeigt noch auf `C:\working\georg\github-runners\` (funktional, aber nicht Infrastructure-Repo)

---

## Kuma Monitoring – GitHub Runner (Nachmittag)

### API Key
- Kuma API Key: `uk1_ughLtEeqWHO905avmezy5j24RVFP5oIrIyOzF6aW` (user_id=1)
- REST API: nur GET /api/monitors verfügbar (v1.23.17); POST geht nicht → Monitor via SQLite direkt eingefügt

### Ergebnis
- Monitor `GitHub Actions Runners (alle 4)` in Kuma aktiv (ID 166) — grün ✅
- URL: `http://runner-status:3099` (intern, Docker-Netzwerk `web`)
- Typ: HTTP, Interval 120s, Retries 3

### runner-status Container
- Image: `python:3-alpine`
- Netzwerk: `web` (gleich wie uptime-kuma)
- Script: `/opt/runner-status/server.py`
- Restart: `unless-stopped`
- Funktion: Proxy zwischen Kuma und GitHub API
  - Kuma → HTTP GET http://runner-status:3099 → Python Script → GitHub API mit PAT
  - Alle 4 Runner online → HTTP 200 (Kuma grün)
  - Mind. 1 offline → HTTP 503 (Kuma rot)

### PAT im Script
- Datei: `/opt/runner-status/server.py` auf Server `91.98.30.140`
- PAT: `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` (hardcoded im Script)
- Bei Rotation: Script auf Server updaten + Container neu starten

### Confluence
- Neue Unterseite: ID `845021194` unter Runner-Seite `844562433`
  - https://minicon.atlassian.net/wiki/spaces/MINICON/pages/845021194

### Kuma Monitoring – Technische Probleme (Learnings)
- Kuma v1.23 REST API hat keine POST /monitors → Direktzugriff auf SQLite nötig
- JSON Query Monitor + Custom Headers → 401-Fehler trotz korrektem Format (bekannter Bug in v1.23)
- Lösung: Proxy-Container dazwischen — Kuma macht nur simplen HTTP-Check

### Windows Runner – Session-Konflikt Fix (vom Vormittag)
- `--replace` beim Registrieren verursacht Session-Konflikte wenn Prozesse noch laufen
- Fix: `.runner`, `.credentials`, `.credentials_rsaparams` löschen, dann ohne `--replace` registrieren
- 97 Ghost-Runner aus Org gelöscht (via GitHub API Batch-Delete)
- Task Scheduler `\Minicon\GitHub-Windows-Runners` war gestoppt → manuell via run-loop.cmd gestartet

### deploy-traefik-routes.yml (vom Vormittag)
- Reusable Workflow `deploy-server-file.yml` in `.github` Repo erstellt (generisch: Datei hochladen + Reload-Command)
- `deploy-traefik-routes.yml` in `infrastructure` Repo: triggert auf routes.yml-Änderungen → auto-deploy
- routes.yml ist jetzt Source of Truth im Repo → direkte Server-Bearbeitung verboten

---

## Session-Fortsetzung 06.03.2026 – 16:24 Uhr

### Confluence-Dokumentation erweitert

#### Kundenportal-Seite (ID 841809922)
- Repo-Link-Banner ergänzt (minicon-kundenportal + minicon-website-service)
- Vollständige Secrets-Tabelle hinzugefügt:
  - website-service: MongoDB, JWT, Stripe Live-Keys + Price IDs, Resend, OpenAI, Cloudflare, GitHub Token, CORS
  - kundenportal Frontend: NEXT_PUBLIC_API_URL

#### Cloudflare Workers CI/CD (ID 845447169)
- GitHub Action `.github/workflows/deploy.yml` erstellt in `Minicon-eG/cloudflare-workers`
- Trigger: Push auf main (geänderte Worker-Verzeichnisse) + workflow_dispatch
- GitHub Secrets gesetzt: CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID
- Confluence-Seite von "kein CI/CD" auf "CI/CD aktiv" aktualisiert

#### CI/CD Runner-Seite (ID 845479937)
- Statische "🟢 online" Spalte entfernt → Verweis auf Uptime Kuma
- Live shields.io-Badge eingebettet (https://img.shields.io/endpoint?url=https://runner-status.minicon.eu/shields)

### runner-status Endpunkt erweitert
- `/shields` Endpoint ergänzt: gibt shields.io-kompatibles JSON zurück
- Zeigt "4/4 online" (grün) oder "X/4 online" (rot)
- YAML-Fix in routes.yml: preview-Routen hatten `\host\` statt `` `host` `` (Backtick-Escaping)
- DNS: runner-status.minicon.eu → 91.98.30.140 (Cloudflare Proxy, A-Record)
- Traefik-Route runner-status.minicon.eu aktiv und erreichbar

### Infrastructure routes.yml Commit
- fc130dd: Fix YAML escape characters + runner-status route
- GitHub Action deployed automatisch auf Server

### Offene Tests
- Mermaid-Diagramm-Test läuft gerade (Seite ID 845971457, "Diagram Test DELETE ME")
- Prüfen ob Mermaid-Makro in Confluence Cloud funktioniert oder PlantUML-PNG-Fallback nötig

---

## Session 22:00–22:23 (Abend)

### Jira API Fix (DAHN Pipeline)
- Alter Endpoint `GET /rest/api/3/search?jql=...` → HTTP 410 Gone (deprecated)
- Gemini Cron-Job `437982fc` gefixt: jetzt `POST /rest/api/3/search/jql` mit JSON-Body
- TOOLS.md hatte den korrekten Endpoint schon dokumentiert
- Confluence Agent-Workflows Seite (846266370) mit Fix-Notiz aktualisiert

### Solution-Map Confluence (835813377) — v20/v21
- Jira DAHN-Board-Link ergänzt (Komponenten-Tabelle + Gemini-Zeile)
- Agent-Tabelle: Cron-Spalten verlinkt auf Agent-Workflows-Seite
- Atlas-Zeile: Builder Guide verlinkt
- Website-Service: Demo-Banner Konzept-Link ergänzt
- Neue Sektion "📋 Prozess-Dokumentation" vor GitHub Repos eingefügt (4 Dokumente)

### DEVELOPMENT-GUIDE.md erstellt
- Pfad: `C:\working\georg\DEVELOPMENT-GUIDE.md`
- Git: `nikolausm/clawdbot-agents`, commits 650d2fb + aa0d84d
- 13 Abschnitte: Tech-Stack, Analyse-Pflicht, Dateistruktur, Bilder, DSGVO, Demo-Banner, Preview-Banner, Speisekarte, Pflichtseiten, Deploy-Vorlage, QA-Checkliste, Kuma-Setup, Lessons Learned
- Zwei-Wege-Verlinkung: Datei ↔ Confluence Builder Guide (846200833)
- DAHN Website Pipeline Cron-Job (76dd7757) aktualisiert: liest Datei als Schritt 2a

### Confluence Dokumentation
- Builder Guide (846200833): Info-Box oben mit Dateipfad + Git-Link
- Agent-Workflows (846266370): Neue Sektion "DEVELOPMENT-GUIDE.md" mit Tabelle + 13 Abschnitte
- Regel: Bei Änderungen an Build-Regeln immer BEIDE aktualisieren (Datei + Confluence)
